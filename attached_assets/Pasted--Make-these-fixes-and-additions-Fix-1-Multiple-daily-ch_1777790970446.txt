"Make these fixes and additions:Fix 1 — Multiple daily check-ins: Allow users to complete a mood and symptom check-in unlimited times per day. Each check-in saves as a separate timestamped Firestore document. In Pattern Intelligence show all check-ins for each day on a timeline so users can see how their mood and energy shifted throughout the day. Label each entry with the time it was submitted.Fix 2 — Flash intensity graph must climb: The live graph on the Live Flash Screen must behave exactly like a mountain curve. From the moment Flash Started is tapped the line must visibly climb upward every second using the elapsed time on the Y axis. When Peak is tapped the line reaches its highest point. During Cooling Down the line slopes downward. At Flash Ended it returns toward baseline. The line must be moving and climbing in real time every single second — not flat. Use requestAnimationFrame to update the SVG path every second based on elapsed time and current stage.Fix 3 — Peak rating timing: Move the intensity rating overlay to AFTER Cooling Down is tapped not when Peak is tapped. When user taps Cooling Down show overlay saying 'Now that the peak has passed — how intense was it? 1 to 5 stars. Totally optional.' with a visible Skip button. Never show rating at Peak tap.Fix 4 — Community content moderation buffer: All community posts must go through a moderation system before appearing publicly. When a user submits any post to any community feed show them a message saying 'Your post is pending review and will appear shortly.' Save post to Firestore with status pending. In the admin dashboard show a moderation queue where admin can approve or reject each post. Only approved posts appear in public feeds. Add automatic filtering that flags posts containing profanity or personal information for immediate admin review. Add community guidelines that users must agree to before first post.Fix 5 — FLASHYAF Score calculation: Calculate the daily FLASHYAF Score 0 to 100 using exactly this formula. Start at 100 and subtract points based on difficulty then add points for positive actions. Subtract 8 points for each flash logged today above the users daily average. Subtract 5 points for each peak intensity rated 4 or 5. Subtract 3 points for each mood check-in rated below 5. Add 10 points for completing a breathing session. Add 5 points for logging all 4 stages on any flash. Add 5 points for submitting a check-in. Add 3 points for opening the app. Cap at 100 minimum at 0. Show the score on the home screen with these messages: 90 to 100 says You are crushing it today, 70 to 89 says Holding strong, 50 to 69 says Rough day — we see you, below 50 says Hard day. You showed up anyway. That counts. Display score calculation breakdown when user taps the score so they understand it.Fix 6 — Body Battery calculation: Calculate Body Battery 0 to 100 using this formula. Start at 100. Subtract 10 for each flash today above the users daily average. Subtract 8 for each peak rated 4 or 5 intensity. Subtract 5 for every mood check-in below 5 out of 10. Subtract 10 if last nights sleep quality check-in was below 5. Add 15 if user completed breathing exercise today. Add 10 if they have had no flashes in the last 3 hours. Add 5 if todays flash count is below their daily average. Show battery as a circular gauge with these labels: 80 to 100 Fully Charged, 60 to 79 Good Energy, 40 to 59 Running Low, below 40 Rest Mode. Explain the calculation when user taps the gauge."